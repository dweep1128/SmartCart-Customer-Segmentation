# Comma-separated origins allowed to call the API (no trailing slash).
# The deployed frontend origin (GitHub Pages) plus local dev servers.
ALLOWED_ORIGINS=https://dweep1128.github.io,http://localhost:5500,http://127.0.0.1:5500

# Port the API binds on (0.0.0.0). Defaults to 8000 if unset.
PORT=8000
